# 本地重构验收记录

日期：2026-09-20。所有验证使用隔离数据；未部署云端、未真实上报状态。

## 功能与兼容

- 全量 unittest：138 项通过（14.1 秒）；新增路径、工厂隔离、兼容入口与发布包验证。
- 46 条原 server 路由按 URL、方法及 endpoint 快照验证；段评和桌宠由既有测试覆盖。
- 92 个迁移函数反向规范化后的 AST 与基线一致，仅文档及 GraphQL 空白差异；子 agent 独立复核。
- 真实 Waitress 启动：非项目工作目录、五条本机 HTTP 接口通过；解压发布包亦通过。
- 临时 SQLite 旧→新→旧读写与在线备份完整性演练通过，无 schema 迁移。
- 管理脚本审查发现的数据目录不一致已修复，补充 env/路径回归。

## 性能对照

同一机器，基线与新版本各 3 个进程，每个接口预热 10 次、采样 200 次；表中为各轮统计量的中位数。测试客户端请求，不包含公网延迟，样例响应 JSON 完全一致。

| 接口 | 原 median / p95（ms） | 新 median / p95（ms） |
|---|---:|---:|
| `/` | 0.1110 / 0.2181 | 0.1103 / 0.1739 |
| `/online_count` | 0.1149 / 0.1924 | 0.1151 / 0.2024 |
| `/get/status_list` | 0.2573 / 0.3564 | 0.2562 / 0.3909 |
| `/blog/views?slugs=test` | 2.1777 / 2.5130 | 2.1713 / 2.5007 |
| `/blog/community/comments/about` | 3.1413 / 3.4776 | 3.1585 / 3.5412 |
| `/music/list` | 0.2361 / 0.3193 | 0.2313 / 0.3220 |
| `/todos` | 0.2471 / 0.3446 | 0.2410 / 0.3687 |
| `/calendar/events` | 0.2452 / 0.3849 | 0.2428 / 0.3287 |

启用 tracemalloc 时，启动约 621 → 636 ms，Python 已分配内存约 15.41 → 15.56 MB（不是进程 RSS）。模块和对象增加有小额成本；接口结果总体接近，不能据此承诺绝对零开销或性能提升。

未做真实外部服务压测、云端并发负载测试或生产切换。SQL、缓存、限流和线程配置保持原实现；未单独计数每个外部请求或 SQL 查询。测试覆盖不能证明不存在未知回归。

## 第二轮独立子代理审查

三个子代理分别审查业务契约、操作兼容并独立测试。发现并修复：

- 首次指定不存在的数据目录时无法创建 data.json；现在创建父目录，并覆盖独立 SLEEPY_DATA_FILE。
- 测试辅助模块依赖 discover 的搜索路径；现在也支持按 tests.test_* 模块单独运行。
- 日志重载测试仍引用旧模块名；更新后重新验证幂等性。

修复后全量 138 项通过（.test-agent-final.log）；按模块运行 50 项通过；最终代码发布包的哈希与解压后真实 Waitress 启动通过。子代理对 92 个迁移方法的 AST 对照未发现额外业务变化。没有部署、生产数据读写或真实上报。
